# 플래너 설정 가이드

한 번만 해두면 됩니다. 전체 15~20분 정도 걸립니다.

---

## 0. 이 앱이 데이터를 두는 곳

| 화면 | 저장 위치 | 휴대폰에서 같이 보이는 앱 |
|---|---|---|
| 월간 일정 | **구글 기본 캘린더** | iOS 기본 캘린더, 구글 캘린더 앱 |
| 위클리 일정 | **분류별 캘린더** (분류 하나 = 캘린더 하나) | 위와 같음. 분류별로 체크를 끄고 켤 수 있음 |
| 할 일 · 쇼핑 리스트 | **구글 Tasks** | Google Tasks 앱, 구글 캘린더 사이드바 |

### 위클리 분류

위클리 일정은 **분류**로 나눠 담습니다. 분류 하나가 구글 캘린더 하나라서,
구글 캘린더나 아이폰 캘린더에서 **분류별로 체크를 끄면 그 분류만 숨길 수 있습니다.**

기본 분류는 `우리딸일정` 이고, 일정을 추가할 때 **분류 → + 새 분류 만들기** 를 고르면
그 이름으로 캘린더가 하나 더 생깁니다. 위클리 화면에는 분류마다 다른 색 점이 붙고,
위쪽에 어떤 색이 어떤 분류인지 알려주는 범례가 나옵니다.

앱은 화면만 그리고 데이터는 전부 구글에 저장됩니다. 그래서 **어느 기기 어느 화면에서 고쳐도 나머지가 전부 같은 값**을 보게 됩니다. 앱을 쓰지 않고 아이폰 기본 캘린더에서 일정을 추가해도 이 앱에 그대로 나타납니다.

---

## 1. 앱을 올려둘 주소 정하기

구글 로그인은 **주소가 있는 웹페이지**에서만 동작합니다. 파일을 더블클릭해서 여는 `file://` 방식으로는 로그인이 되지 않습니다.

### 방법 A — GitHub Pages (무료, 권장)

GitHub Pages는 **"리포지토리에 올려둔 파일을 그대로 웹사이트로 공개해 주는 기능"** 입니다. 서버를 빌리지 않아도 주소가 하나 생깁니다.

#### 1) 리포지토리 만들기

[github.com](https://github.com) 로그인 → 오른쪽 위 **+** → **New repository**

- Repository name : `planner`
- **Public** 선택 — Private으로 두면 Pages가 동작하지 않습니다
- **Create repository**

#### 2) 파일 올리기

비어 있는 화면에 뜨는 **uploading an existing file** 링크를 누릅니다.
`planner-web` 폴더를 열고 **그 안의 내용물 전부**를 끌어다 놓으세요.

```
index.html   app.js   app.css   config.js
korean-calendar.js   manifest.webmanifest   sw.js   icons(폴더)
```

아래로 내려 **Commit changes** 클릭.

> ⚠️ `planner-web` 폴더를 통째로 올리면 주소가 한 단계 깊어져 동작하지 않습니다.
> 업로드 후 파일 목록 **맨 위에 `index.html` 이 보이면** 제대로 올라간 것입니다.

#### 3) Pages 켜기 — 물어보신 단계입니다

리포지토리 상단 탭에서 **Settings**(톱니바퀴 아이콘) → 왼쪽 메뉴를 내려 **Pages** 클릭.

`Build and deployment` 아래에 선택 칸이 나옵니다. 각각 이런 뜻입니다.

| 칸 | 고를 값 | 무슨 뜻인가 |
|---|---|---|
| **Source** | `Deploy from a branch` | "따로 빌드하지 말고 올려둔 파일을 그대로 웹사이트로 써라"는 뜻입니다. 다른 선택지 `GitHub Actions`는 빌드가 필요한 프로젝트용이라 우리에겐 해당 없습니다 |
| **Branch** (왼쪽 칸) | `main` | 파일이 올라가 있는 가지(버전 줄기) 이름입니다. 방금 만든 리포지토리라면 `main` 하나뿐입니다 |
| **Branch** (오른쪽 칸) | `/ (root)` | 웹사이트의 첫 페이지를 **어느 폴더에서 찾을지**입니다. `root`는 최상단이라는 뜻이고, 우리는 `index.html`을 최상단에 뒀으니 이걸 고릅니다 |

오른쪽 **Save** 클릭.

#### 4) 주소 확인

1~2분 기다린 뒤 **Settings → Pages** 를 새로고침하면 위쪽에 초록색 상자로 주소가 뜹니다.

```
https://eunok26-lgtm.github.io/planner/
```

이 주소를 적어두세요. 아래 3단계에서 씁니다.

### 방법 B — 내 PC에서만 먼저 시험

```bash
python -m http.server 8080 --directory planner-web
```

브라우저에서 `http://localhost:8080` 으로 접속합니다. 이 주소도 구글이 허용해 줍니다.

---

## 2. 구글에서 API 사용 설정

[console.cloud.google.com](https://console.cloud.google.com) 접속 (구글 계정으로 로그인)

1. 상단 프로젝트 선택 → **새 프로젝트** → 이름 `플래너` → 만들기
2. 왼쪽 메뉴 **API 및 서비스 → 라이브러리**
   - `Google Calendar API` 검색 → **사용** 클릭
   - `Google Tasks API` 검색 → **사용** 클릭

> 💳 상단에 **"$300 무료 크레딧으로 사용해 보기"** 배너가 뜨더라도 **누르지 마세요.**
> 모든 신규 계정에 보이는 광고성 안내일 뿐입니다. 이 두 API는 결제 계정 없이 무료로 쓸 수 있고,
> 개인이 자기 캘린더를 읽고 쓰는 정도는 무료 할당량 안에서 충분합니다.

### OAuth 동의 화면 (= "Google 인증 플랫폼")

3. 왼쪽 메뉴 **API 및 서비스 → OAuth 동의 화면**

   *"Google 인증 플랫폼이 아직 구성되지 않음"* 이라고 나오면 정상입니다.
   **시작하기** 를 누르면 4단계짜리 입력창이 뜹니다.

   | 단계 | 입력할 값 |
   |---|---|
   | 앱 정보 | 앱 이름 `플래너` / 사용자 지원 이메일 → 드롭다운에서 본인 주소 |
   | 대상 | **외부 (External)** |
   | 연락처 정보 | 본인 이메일 주소 |
   | 완료 | "사용자 데이터 정책에 동의합니다" 체크 → **만들기** |

   > 개인 지메일 계정이면 **외부**만 선택할 수 있습니다. **내부**는 회사 Workspace 계정 전용입니다.

4. 만들기를 마치면 왼쪽에 메뉴가 생깁니다. **대상(Audience)** 클릭
   → 아래 **테스트 사용자** 항목 → **+ Add users** → **본인 지메일 주소** 입력 → 저장

> ⚠️ 같은 화면의 **"게시" 버튼은 누르지 마세요.** 개인용이라면 테스트 상태가 낫습니다.
> 테스트 사용자로 등록한 본인 계정은 제한 없이 쓸 수 있고, 게시하면 오히려 구글 검토 절차가 붙습니다.

---

## 3. 클라이언트 ID 발급

5. 왼쪽 메뉴 **클라이언트(Clients)** → **+ 클라이언트 만들기**
   - 애플리케이션 유형 : **웹 애플리케이션**
   - 이름 : `플래너 웹`
   - **승인된 JavaScript 원본** — `+ URI 추가` 를 눌러 아래 두 개를 넣습니다

     ```
     https://eunok26-lgtm.github.io
     http://localhost:8080
     ```

     ⚠️ 끝에 슬래시(`/`)나 경로(`/planner`)를 붙이면 안 됩니다. **도메인까지만** 넣으세요.
   - **승인된 리디렉션 URI** — 비워 둡니다. 이 앱은 리디렉션을 쓰지 않습니다.
   - **만들기** → 창에 나타나는 **클라이언트 ID** 를 복사

> 클라이언트 ID는 이렇게 생긴 **긴 문자열**입니다. 짧은 이름이 나왔다면 그건 클라이언트 ID가 아닙니다.
> ```
> 743920184756-a8f3k2m9qb1c7d5e0g4h6j.apps.googleusercontent.com
> ```
> 항상 `.apps.googleusercontent.com` 으로 끝납니다.

6. `config.js` 를 열어 첫 번째 값에 붙여넣습니다.

```js
GOOGLE_CLIENT_ID: '743920184756-a8f3k2m9qb1c7d5e0g4h6j.apps.googleusercontent.com',
```

GitHub Pages를 쓴다면 수정한 `config.js` 를 다시 업로드하세요.

---

## 4. 홈 화면에 앱으로 설치

### 아이폰 · 아이패드
Safari(크롬 아님)로 주소 접속 → 하단 **공유** 버튼 → **홈 화면에 추가**

### 안드로이드 · PC
Chrome으로 접속 → 주소창 오른쪽 **설치** 아이콘 (또는 메뉴 → 앱 설치)

설치하면 주소창 없이 전체화면으로 뜨고, 오프라인에서도 마지막으로 본 내용이 열립니다.

---

## 5. 처음 로그인할 때

**"확인되지 않은 앱"** 또는 **"Google에서 확인하지 않은 앱"** 경고가 뜹니다. 본인이 만든 앱이고 테스트 사용자로 본인만 등록되어 있으니 정상입니다.

→ **고급** → **플래너(안전하지 않음)로 이동** → 권한 허용

---

## 사용법

| 하고 싶은 것 | 방법 |
|---|---|
| 일정 추가 | 월간에서 날짜 칸을 누르거나, 위클리에서 빈 줄을 누릅니다 |
| 일정 수정·삭제 | 일정 이름을 누르면 편집 창이 뜹니다 |
| 위클리 분류 나누기 | 편집 창의 **분류** 에서 고릅니다. **+ 새 분류 만들기** 로 새 분류(=새 캘린더)를 추가 |
| 일정을 다른 분류로 옮기기 | 편집 창에서 분류만 바꿔 저장 (반복 일정은 옮길 수 없습니다) |
| 종이에 인쇄 | 위클리 화면 → **인쇄** → 몇 주치와 용지를 고릅니다. **A4 가로 한 장에 두 주**가 들어가고 자르는 선이 표시됩니다 |
| 쇼핑·할 일 수정 | 항목 이름을 누르면 수정 창이 뜹니다 |
| 할 일 추가 | 오늘 화면 → 할 일 옆 **+ 추가** |
| 쇼핑 항목 추가 | 쇼핑 화면 → 입력 후 Enter (분류 선택 가능) |
| 다른 기기 변경사항 반영 | 앱으로 돌아오면 자동으로 다시 불러옵니다 |

---

## 손볼 만한 곳

| 파일 | 내용 |
|---|---|
| `config.js` | 클라이언트 ID, 위클리 기본 분류 이름, 쇼핑 분류 항목, 주 시작 요일(월/일) |
| `korean-calendar.js` | 공휴일 · 절기 표. 연도를 추가하려면 객체에 키를 하나 더 넣으면 됩니다 |
| `app.css` | 색상. 최상단 `:root` 의 `--hol`, `--sun`, `--sat` 이 인쇄물과 같은 값입니다 |

---

## 잘 안 될 때

| 증상 | 원인과 해결 |
|---|---|
| `origin_mismatch` 오류 | 3단계의 승인된 JavaScript 원본이 실제 접속 주소와 다릅니다. 슬래시·경로 없이 도메인만 넣었는지 확인하세요 |
| 로그인 창이 안 뜸 | 팝업 차단을 해제하세요 |
| `file://` 로 열었더니 흰 화면 | 반드시 `https://` 또는 `http://localhost` 로 접속해야 합니다 |
| 일정이 안 보임 | `config.js` 의 `CALENDAR_ID` 가 `'primary'` 인지, 로그인한 계정이 맞는지 확인 |
| 수정했는데 화면이 그대로 | 서비스 워커 캐시입니다. 새로고침을 두 번 하거나 앱을 껐다 켜세요 |
| 절기 날짜가 하루 다름 | `korean-calendar.js` 의 `KR_TERMS` 에서 직접 고치면 됩니다 |

---

## 부록 · 네이버 캘린더 일정 옮겨오기 (한 번만)

기존에 네이버 캘린더에 쌓아둔 일정을 구글로 옮기는 절차입니다.
개인용 네이버 캘린더는 외부에서 읽고 쓰는 통로가 없어 실시간 연동은 불가능하고,
파일로 한 번 옮긴 뒤 앞으로는 이 앱이나 구글 캘린더에서 입력하는 방식이 됩니다.

### 1) 네이버에서 내보내기

**PC 웹브라우저**로 [calendar.naver.com](https://calendar.naver.com) 접속
(모바일 앱에는 이 기능이 없습니다)

1. 왼쪽 사이드바의 **"내 캘린더"** 글자 위에 마우스를 올리면 **톱니 아이콘**이 나타납니다 → 클릭
   (평소에는 숨어 있다가 마우스를 올려야 보입니다)
2. **일정설정** 탭
3. **내보내기(백업)** 클릭
4. 기간은 **전체 일정**을 고르고, 내보낼 캘린더를 선택 → 저장

`.ics` 파일이 받아집니다. 압축(zip)으로 받아졌다면 풀어서 안의 `.ics` 를 꺼내 두세요.

### 2) 구글로 가져오기

[calendar.google.com](https://calendar.google.com) → 우측 상단 **톱니 → 설정**
→ 왼쪽 메뉴 **가져오기/내보내기**

- **컴퓨터에서 파일 선택** → 위에서 받은 `.ics`
- **캘린더에 추가** → **기본 캘린더** (본인 이메일 이름으로 된 것)
- **가져오기**

> ⚠️ 대상은 반드시 **기본 캘린더**로. 이 앱은 기본 캘린더(`primary`)만 읽습니다.
> ⚠️ 같은 파일을 두 번 가져오면 일정이 **중복**됩니다. 한 번만 실행하세요.

### 3) 가져온 뒤

가져온 일정에는 출처 표시가 없어 **월간 화면**에 나타납니다. 위클리에는 보이지 않습니다.

네이버 캘린더에서도 계속 보고 싶다면, 구글 캘린더 설정에서 **비공개 iCal 주소**를 복사해
네이버 캘린더에 **외부 캘린더 구독**으로 등록하면 됩니다 (네이버에서는 읽기 전용).
